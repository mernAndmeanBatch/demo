const First = (props) => {
    return (
        <>
            <span>{props.name}</span>
            <span>{props.age}</span>
            <span>{props.fee}</span>
            <br />
        </>

    )
}

export default First